name: Setup

runs:
  using: composite
  steps:
    - uses: actions/setup-node@v3
      with:
        node-version: 18.15

    - run: npm install -g yarn
      shell: bash

    - run: yarn
      shell: bash
